<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    
    <!-- Console appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- File appender for performance metrics -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/apexflow-example.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
    
    <!-- Specific logger configurations -->
    <logger name="dev.waylon.apexflow" level="DEBUG" />
    <logger name="org.apache.pdfbox" level="WARN" />
    <logger name="org.apache.commons" level="WARN" />
    <logger name="java.awt" level="WARN" />
    <logger name="javax.imageio" level="WARN" />
    
</configuration>